/** Centering policy for AdwHeaderBar. */
export type AdwCenteringPolicy = "loose" | "strict";

/** Toolbar visual style. */
export type AdwToolbarStyle = "flat" | "raised" | "raised-border";

/** Response button appearance in AdwAlertDialog. */
export type AdwResponseAppearance = "default" | "suggested" | "destructive";

/** Navigation direction. */
export type AdwNavigationDirection = "back" | "forward";

/** Toast priority. */
export type AdwToastPriority = "normal" | "high";

/** Dialog adaptive presentation mode. */
export type AdwDialogPresentationMode = "auto" | "floating" | "bottom-sheet";

/** Length unit for responsive sizing. */
export type AdwLengthUnit = "px" | "pt" | "sp";
